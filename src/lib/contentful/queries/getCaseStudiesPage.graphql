query CaseStudiesPage($slug: String!) {
  caseStudiesPageCollection(where: { slug: $slug },limit: 1) {
    items {
      ...PageIntros

      hero {
        __typename 
        ...HeroFields
      }
    }
  }
}